# domain-companies

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-companies` to build the library.

## Running unit tests

Run `nx test domain-companies` to execute the unit tests via [Jest](https://jestjs.io).
