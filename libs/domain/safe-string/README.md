# safe-string

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build safe-string` to build the library.

## Running unit tests

Run `nx test safe-string` to execute the unit tests via [Jest](https://jestjs.io).
