# infra-companies

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infra-companies` to build the library.

## Running unit tests

Run `nx test infra-companies` to execute the unit tests via [Jest](https://jestjs.io).
