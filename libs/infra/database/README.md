# infra-database

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infra-database` to build the library.

## Running unit tests

Run `nx test infra-database` to execute the unit tests via [Jest](https://jestjs.io).
